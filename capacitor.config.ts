import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'design_proj_dec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
